# Project Memory
最終更新: 2026-09-06 (store screenshots 2-4 captured, first-sheet named-range fix)

## 現在の状態
Scopion = formula auditor for Google Sheets, an ACE (Excel VBA) port. Two builds:
- Apps Script (frozen): bound script on test sheet 11GMudQJ1VmzTgscq4sY82ceb1lHywWKisegzQPQC3P0, macro Cmd+Option+Shift+1, Karabiner rule Cmd+Shift+A. Row/column neighbour flag shipped there (dac2d5a), not yet ported to the extension. Deploy = edit monaco in the editor tab, verify the checksum before saving (string transport corrupts characters).
- Chrome extension (`extension/`, MV3) = the product; main == feat/chrome-extension (worktree), see git log, pushed to github.com/dT-Tb-labs/Scopion (public). Manifest 1.0.0, `_locales` en/ja, onboarding.html. User-verified live 2026-09-05: Ctrl+Shift+A + toolbar icon, origin row first, ↑↓ walk with the selection following, range rows select the whole block, → drill, ←/Backspace back, Enter close-and-stay, Esc return-and-close, liquid-glass panel, formula-token ↔ row colour link, cross-sheet rows bold, smart placement (right → below → above), breadcrumbs in the box beside OK (click = back to that origin), hidden sheets walked by default and re-hidden on close via batchUpdate (OAuth: `spreadsheets.readonly` in the manifest since c77ce19; the `spreadsheets` edit scope is requested incrementally via chrome.identity `scopes` for the re-hide, declined → panel stays open with a re-hide-by-hand notice; a declined base consent is not re-prompted per open), DOM-only mode when the Sheets API refuses the file (.xlsx opened in Sheets: values "—", jumps work). Scorpius icon: extension/icons (`swift extension/icons/render.swift scorpio.svg extension/icons`). Store description ≤132 chars, aimed at financial modellers.
Spec: docs/superpowers/specs/2026-09-03-scopion-chrome-extension-design.md. Plan: docs/superpowers/plans/2026-09-03-scopion-chrome-extension.md.

## 直近の作業
2026-09-06 pm: store screenshots 2-4 captured (docs/store/assets, recipe in listing.md), dist/scopion-1.0.0.zip rebuilt (18 files). Fixed 94c791c: the Sheets API omits GridRange.sheetId when it is 0, so every named range on the FIRST sheet was dropped (Model!B6 showed Growth as unknown) — adapter.js defaults to sheet 0; live-verified after the fix (names Growth/Shift arrive, B2 row shown). Chrome MCP lessons: `osascript -e 'tell application "Google Chrome" to set bounds of window id N to {0,39,1280,1016}'` works with the sandbox off (no Accessibility needed) and gives the 1280×800 viewport at Y=216; MCP `computer` clicks land ~50px low on this page (a click meant for the name box typed into A1 — undone with Cmd+Z), so drive jumps via JS on #t-name-box (focus, value, keydown Enter) and open via the scopion:toggle event; MCP `key` needs `#scopion-host.shadowRoot.querySelector('.list').focus()` before every arrow (the panel loses focus between MCP calls); MCP `hover` moves the real cursor (park at 1279,799 before screencapture). `?hl=en` does not switch the Sheets UI language (account setting wins), so all shots are JA UI. Toggling while the panel is open = drill (breadcrumb grows) — close with Esc first. Commit trailers: the policy guard rejects Co-Authored-By; commit without it.
Worktree .worktrees/chrome-extension (branch feat/chrome-extension) is KEPT on purpose: the user's Chrome loads the extension unpacked from `.worktrees/chrome-extension/extension` (id ihjnpijodijcbigekdihoamchmkgdgie, GCP project 839296398878; oauth.local.json is in both checkouts, scopion.pem only in the worktree — needed only to regenerate the key). Dev reload (9b37dc4, live-verified 2026-09-06): run `node extension/build.js --watch` IN THE WORKTREE; it rebuilds and stamps extension/dev-reload.json, background.js importScripts dev-reload.js (not in SHIP) which polls the stamp, chrome.runtime.reload()s and reloads the Sheets tabs — no chrome://extensions ↻ needed. fs.watch is refused inside the sandbox (EMFILE) so it uses fs.watchFile; *.pem unreadable there. A manifest/scope change still re-prompts consent.
Verification: `node run_tests.js` (35/35), `node run_e2e.js`, `node extension/build.js --lib-only && node --test extension/test/*.test.js` (53; the `--test <dir>` form is broken on Node 26.5 here); `node extension/build.js --pack` → dist/scopion-1.0.0.zip (18 files). Smoke checklist: tests/README.md. Automation trigger: `document.dispatchEvent(new CustomEvent('scopion:toggle'))`; debug trace in `#scopion-host` dataset.trace. build.test.js parks and restores a real manifest.json.
Sheets DOM facts (verified live): `#t-name-box` = address / range / covering named-range name; `#t-formula-bar-input` = formula; `.active-cell-border` ×4 = cell rect, `.selection-border` ×4 = range rect (hidden pieces are 0×0 at the page corner — skip); jump = name box + Enter (`'Sheet'!A1`, `B1:B3`, bare names; a hidden sheet gets unhidden); `#waffle-rich-text-editor`.focus() returns the keyboard to the grid. API enum: ErrorValue.type is DIVIDE_BY_ZERO. Sheets API refuses .xlsx-in-Sheets documents.
Test sheet sheets: Model, Inputs (constants, lookup table), Calc (50 formulas: cross-sheet, hidden, OFFSET/INDEX/INDIRECT, names, blanks, whole col/row, LET, ARRAYFORMULA, errors), Hidden Calc.
Known limits: MATCH exact-only; `=SUM(INDEX(..):INDEX(..))` reports endpoints; the Apps Script sidebar iframe is unreachable from automation (walk checked via tests/harness.html).

## 次のタスク / 未解決
- Chrome Web Store competitors (2026-09-05): SheetTrace (DOM-based precedents, free), SheetWhiz + Formula Explorer (arrow-key navigation, Goal Seek/What-If, YC), XLKeys, ShortieCuts. Scopion lacks Dependents (dropped on purpose) — a weakness for financial modellers; differentiators: origin row/range selection, token colour link, hidden re-hide, xlsx mode, breadcrumbs.
- Web Store release slice committed on main as 2727f75 (babel store-listing, M: 1 round + re-review + critic, report .babel/store-listing/acceptance-report.md): _locales en/ja + __MSG__ manifest 1.0.0, onboarding.html/js (onInstalled install-once; toolbar click with no reply → onboarding, #reload on /d/ URLs; content.js replies {ok:true}), docs/store/listing.{en,ja,md}, docs/{index,privacy}.md + _config.yml for GitHub Pages (repo pushed public 2026-09-05 as github.com/dT-Tb-labs/Scopion, remote origin; Pages enabled main:/docs → https://dt-tb-labs.github.io/Scopion/ and /privacy/), assets: promo-440x280.png (Playwright from promo-tile.html) + screenshot-1.png (JA UI; retake with ?hl=en, capture.sh N X Y, X=0 Y=216 when the viewport is 1280×800 at the top-left). Tests 51/51. Worktree fast-forwarded to main (6124704) and rebuilt; live-verified 2026-09-05: ja Chrome shows the Japanese manifest strings, reload (reason update) opened no tab, Ctrl+Shift+A opens the panel, onboarding.html#reload renders in Japanese. Live-verified 2026-09-06 (this account, full scope already granted): incremental-scope build c77ce19 reloaded, Calc!B7 → ↓ unhid Hidden Calc with the notice '…(asks for edit permission the first time)', Esc returned to Calc!B7 and Hidden Calc was hidden again (#scopion-host persists after close by design, win.hidden). NOT verified: the toolbar-icon click path (needs a real click). The account already granted the full scope, so no new consent will appear here; a fresh Google account is the only real first-run test. chrome://extensions warns that extension/scopion.pem sits inside the extension dir — harmless, --pack excludes it; move it out to silence. Search Console 2026-09-06: URL-prefix properties https://dt-tb-labs.github.io/Scopion/ AND https://dt-tb-labs.github.io/ both verified with docs/google1008eb5149c74df6.html (same file at the root of the new public repo dT-Tb-labs/dT-Tb-labs.github.io — keep both). GCP Branding is filled and saved (name, logo, homepage, privacy, domain), but 'ブランディングを確認' → '問題は修正した' → 続行 still reports 'ホームページの URL … のウェブサイトが登録されていません' immediately (evening 2026-09-06) — likely propagation delay: retry next day, then use '検出された問題は正しくないと思う' (manual review). Verification Center's 'Prepare for verification' stays greyed until branding passes. Next: Audience → In production, Data Access → both scopes, then upload dist/scopion-1.0.0.zip without --pem and re-point the OAuth client if the Item ID differs; retake screenshots with the EN UI only if the user switches the account language. Chrome MCP lessons: the tab must be the window's active tab for screencapture; tabs_create opens background tabs; MCP cannot navigate chrome:// or run JS on chrome-extension:// pages; System Events keystrokes work (Cmd+L + URL, Cmd+9, Ctrl+Shift+A) but `click at` is refused (no Accessibility for osascript) — reload unpacked extensions via chrome://extensions → Tab×4 → Enter on 更新; always `open -a "Google Chrome"` first or keys go to the front app.
- Web Store (2026-09-05 audit): `node extension/build.js --pack [--pem extension/scopion.pem]` → dist/scopion-<v>.zip (ship list only, manifest `key` stripped, key.pem in zip keeps the ID — unverified against Google docs, check the Item ID after the first upload). docs/store/PRIVACY.md (Limited Use text) + listing.md (justifications) still need a public URL; still needs OAuth verification of `spreadsheets`, 1280×800 screenshots, 440×280 promo tile. icon128 now 96px art + 16px padding (store rule). Port the neighbour flag (3×3 block fetch); consider Dependents via API scan.
- Parked: dom.js jump settle accepts any name-shaped name-box value (narrow race on cross-sheet jumps into named cells) — compare against namesCovering() if it ever bites.
- Scratch dirs to delete when the user agrees: `.superpowers/` (main) and `.worktrees/chrome-extension/.superpowers/` (git-ignored SDD ledgers).
